import styled from 'styled-components';

const DemoWrapper = styled.div`
  height: calc(100vh - (200px + 64px + 48px + 24px));
  padding-top: 32px;
`;

// eslint-disable-next-line import/prefer-default-export
export { DemoWrapper };
