import styled from 'styled-components';

const BottombarWrapper = styled.div`
  position: absolute;
  bottom: 0;
  height: 200px;
  border-top: 1px solid #e1e1e1;
  width: calc(100% + 48 + 48);
  left: 0;
  padding: 24px 48px;
`;

// eslint-disable-next-line import/prefer-default-export
export { BottombarWrapper };
